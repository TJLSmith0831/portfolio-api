TODO.txt — Speed up /summarize_player_fit + add timing logs
====================================================

Driver + concurrency
--------------------
[X] Create app/scrapers/driver_singleton.py with:
    - a module-level Playwright driver singleton
    - a module-level threading.Lock()
    - get_driver() that initializes Chrome only once
[X] In summarize_player_fit endpoint:
    - delete per-request webdriver.Chrome(...) creation
    - delete driver.quit() in finally
    - replace with: driver = get_driver()
    - wrap ALL playwright usage in: with driver_lock:
[X] Ensure you run uvicorn with ONE worker (no --workers > 1).

LLM client reuse + model pinning
-------------------------------
[X] Pin model tag: change OllamaModels.LLAMA from "llama3.2" / ":latest" to "llama3.2:1b" (or ":3b" if you must).
[X] Create a singleton LLMClient (don’t instantiate inside the request handler).
[X] In _chat_with_retries, add:
    - temperature=0
    - max_tokens=500 (tune per response size)
[X] Migrate to job-based framework to improve latency and handle multiple requests at once.
    [X] Install Redis
    [X] Write Redis store tests
    [X] Create Redis store for job queues
    [X] Update endpoint to return job ID
    [X] Add BackgroundTask to process job
    [X] Create a job queue and worker
    [X] Add job status endpoint
    [X] Update framework to send final output to UI when completed

Start Ollama inside the container + keep warm
--------------------------------------------
[X] Add /app/entrypoint.sh that:
    - starts `ollama serve` in background
    - pulls your pinned model (ollama pull llama3.2:1b)
    - then starts uvicorn
[X] In Dockerfile:
    - COPY entrypoint.sh and chmod +x
    - switch CMD to run /app/entrypoint.sh
[X] Add env vars in Dockerfile (or runtime):
    - OLLAMA_KEEP_ALIVE=10m
    - OLLAMA_NUM_PARALLEL=1
    - OLLAMA_MAX_LOADED_MODELS=1
[ ] Add a FastAPI startup warmup call (1 tiny chat) so first request isn’t slow.

Add timing logs (measure before/after each change)
--------------------------------------------------
[X] Create app/utils/timing.py and add to API relevant paths
[X] Add req_id + timers in summarize_player_fit

Optional (big win)
------------------
[X] Collapse the two LLM calls into one (extract+fit in a single response) if still slow.
[X] Rename ollama_client.py into llm_client.py.

Done criteria
-------------
[X] Run 5 warm requests and record median + p95 from logs.
[X] Confirm first request after restart is close to warm request time (warmup + keepalive working).
